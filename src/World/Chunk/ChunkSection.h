/*#include "../World.h"

class ChunkSection {
    friend class Chunk;
    public:
        ChunkSection(World& world);
        ~ChunkSection() = default;
        Chunk* getChunkBlock();
    private:
        World m_pWorld;
};*/